<h1>Rename group '<%= @group.name %>'</h1>

<%= form_for @group do |f| %>

    <p><%= f.text_field :name %></p>
    <p><%= f.submit "Submit" %></p>

<% end %>

<p><%= link_to "Back to groups", groups_path %></p>
